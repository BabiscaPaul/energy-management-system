api:
  dashboard: true
  insecure: true  # For development only

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: ems-network

log:
  level: INFO
